# givp for Python

Python distribution of the **givp** metaheuristic optimizer.

The source code, tests, dependency lock and development tooling are intentionally
self-contained in this directory. For documentation and the other language
implementations, see the repository root README.
